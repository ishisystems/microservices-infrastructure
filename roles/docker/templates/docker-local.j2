# /etc/sysconfig/docker-local
# Generated by Ansible

other_arg='--dns {{ private_ipv4 }} --dns-search service.{{ consul_dns_domain }} --log-driver=syslog'

HTTP_PROXY="http://{{ docker_proxy }}"
HTTPS_PROXY="https://{{ docker_proxy }}"
http_proxy="${HTTP_PROXY}"
https_proxy="${HTTPS_PROXY}"
no_proxy="{{ docker_no_proxy_domains }}"
